# loan-transaction

Simple Go API for loan tracking (MVP).

## Requirements
- Go (local dev)
- Docker (optional)

## Setup
Copy env:
```bash
cp .env.example .env
# then edit .env and set DATABASE_URL
